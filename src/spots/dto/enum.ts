export enum SpotStatus {
  available = 'available',
  reserved = 'reserved',
  occupied = 'occupied',
}

export enum TicketStatus {
  reserved = 'reserved',
  used = 'used',
  cancelled = 'cancelled',
}
