export enum EventType {
  GENDER_SPECIFIC = 'gender_specific',
  GENERAL = 'general',
}

export enum UserType {
  CLIENT = 'client',
  STAFF = 'staff',
}

export enum Gender {
  MALE = 'male',
  FEMALE = 'female',
}
